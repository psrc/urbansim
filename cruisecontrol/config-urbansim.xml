<cruisecontrol>
  <project name="urbansim" buildafterfailed="False">
    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt" />
    </listeners>
    <bootstrappers>
      <cvsbootstrapper localWorkingCopy="projects/${project.name}" />
    </bootstrappers>
    <modificationset quietperiod="${quietperiod}">
      <cvs localWorkingCopy="projects/${project.name}" />
      <buildstatus logdir="logs/opus_core" />
    </modificationset>
    <schedule interval="${interval}">
      <ant anthome="${ant.home}" buildfile="build.xml" target="run-build">
      	<property name="buildpath" value="projects/${project.name}" />
      </ant>
    </schedule>
    <log>
      <merge dir="projects/${project.name}/tests" />
    </log>
    <publishers>
      <antpublisher Target="trafficlights-finish" BuildFile="build.xml" anthome="${ant.home}" />
    </publishers>
  </project>
</cruisecontrol>